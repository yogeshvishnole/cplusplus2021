#include <iostream>

int main()
{

    // This is conditions and branches

    // there are two part of programming
    // 1.mathematical  fast
    // 2.logical slow than math because jum here an dthere in memory so a little overhead
    const char *ptr = nullptr;
    if (ptr)
        std::cout << "Hello";
}