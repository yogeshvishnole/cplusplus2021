#include <iostream>
using namespace std;

int main()
{
    //    Control flow

    //  Three keywords
    // 1. Continue move to next iteration
    // 2. Break --> break out of the for loop
    // 3. return --> return out of the function

    // continue and break works the same in all type of loops

    // we can write return anywhere in the function
    // code below return is dead code
    // some compilers not allow dead code they simply throws error
    // We are done with fundamentals now
}